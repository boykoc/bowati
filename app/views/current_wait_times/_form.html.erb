<%= form_for(@current_wait_time) do |f| %>
  <% if @current_wait_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_wait_time.errors.count, "error") %> prohibited this current_wait_time from being saved:</h2>

      <ul>
      <% @current_wait_time.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
